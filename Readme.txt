Tools:
Editor: notepad++
Nodejs version: 8.9.1
Redis server

Commands:
npm init ### "will create package.json in the root of folder"
npm install express --save ### "Install express and save into node_modules folder" 
npm install redis --save ### "Install redis and save into node_modules folder"
npm install --save socket.io ### "Install socketio and save into node_modules folder"

Angular
npm install -g @angular/cli